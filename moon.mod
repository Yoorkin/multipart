name = "Yoorkin/multipart"

version = "0.1.1"

readme = "README.mbt.md"

repository = "https://github.com/Yoorkin/multipart"

license = "Apache-2.0"

keywords = [ "multipart", "form-data" ]

preferred_target = "wasm"

description = "A streaming multipart reader/writer library integrated with `moonbitlang/async`."

import {
  "moonbitlang/async@0.22.1",
}
